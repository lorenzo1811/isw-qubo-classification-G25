# LLM Interaction Log

This file records all relevant interactions with Large Language Models (LLMs) used during the project.

Each interaction must be recorded as a separate entry. You must use the following unique interaction ID: `G03-06-12-001`, `G03-06-12-002`, etc., where GXX is the group identifier, MM-DD means month and day, followed by a progressive number starting from 001 for the first recording of the day.

Do **not** overwrite previous entries. Do **not** use the same downloaded file name twice. All downloaded files must be stored in the project repository and referenced exactly with their relative path.

The same day must not be covered by more than one interaction log. If you feel that the interaction log is becoming too long, at the end of the day close the log and start another one the following day. The logs are identified by their log_id, with the group code and a progressive number NN: "GXX-NN". Each log must be contained in a file named: "LOG-GXX-NN.md". 

---

## Metadata

```yaml
group_id: "G25"
repository_url: "https://github.com/lorenzo1811/isw-qubo-classification-G25"
students:
  - matricola: "60/61/66454"
    name: "Lorenzo Scanu"
  - matricola: "60/61/66515"
    name: "Federico Pitzalis"
last_update: "2026-07-17"
log_id: "G25-03"
```

---

# Interaction Entries

---

## Interaction G25-07-17-001

### 1. LLM and chat information

```yaml
llm_name: "Claude"
llm_version_or_model: "Claude Sonnet 5"
chat_name_or_identifier: "Training e classificazione"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "66454"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:  
  - "classification_model"  
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
I am continuing my binary classification project. After feature selection, I now 
have reduced training and test CSV datasets (selected features + target column). 
I need to implement the classification model module.

I need a file "model.py" containing two mandatory functions.

Function 1 - training:

def train(
    classifier: str,          # which classifier to use
    reducedTrain_csv: str,    # path to the reduced training dataset
    target_column: str,       # name of the target column
    model_path: str,          # path to save the trained model
    metrics_json: str,        # path to save training statistics
    seed: int = 42,
):

Requirements:
- Support three classifiers, selectable by the "classifier" string parameter: 
  "random_forest" (mandatory), plus two more of my choice (I'll use 
  "logistic_regression" and "gradient_boosting"), all from scikit-learn.
- Read the reduced training dataset, train the selected classifier on all 
  columns except target_column (which is the label), save the trained model to 
  model_path using joblib.
- Save training statistics to metrics_json with exactly this structure (example 
  values):
  {
      "classifier": "random_forest",
      "seed": 42,
      "training_dataset": "training_reduced.csv",
      "target_column": "target",
      "model_path": "model80.joblib",
      "n_samples": 14000,
      "n_features": 19,
      "target_1_percentage": 1.59,
      "dataset_input_time": 0.16,
      "training_time": 1.61
  }

Function 2 - prediction:

def predict(
    reduced_Test_csv: str,     # path to the reduced test dataset
    target_column: str,        # name of the target column
    model_path: str,           # path to the saved trained model
    predictions_csv: str,      # path to save per-record predictions
    classif_stats_json: str,   # path to save classification statistics
):

Requirements:
- Load the reduced test dataset and the trained model (joblib).
- Generate predictions (predicted class and probability/score for the positive 
  class).
- Save predictions_csv with one row per test record, columns: row_n (row index), 
  target (real class), prediction (predicted class), score (probability of 
  positive class). Example:
  row_n,target,prediction,score
  0,1,1,0.83
  1,0,1,0.21
  2,1,1,0.77
- Save classif_stats_json with exactly this structure (example values):
  {
      "classifier": "random_forest",
      "n_samples": 6000,
      "target_1_count": 375,
      "target_1_percentage": 1.56,
      "accuracy": 0.9914166666666666,
      "class_0": {
          "precision": 0.9926789245592629,
          "recall": 0.9986455026455027,
          "f1": 0.9956532748143146,
          "support": 23625
      },
      "class_1": {
          "precision": 0.8626609442060086,
          "recall": 0.536,
          "f1": 0.6611842105263158,
          "support": 375
      },
      "roc_auc": 0.9663519153439154,
      "confusion_matrix": {
          "labels": [0, 1],
          "matrix": [[23593, 32], [174, 201]]
      }
  }

The module must also be runnable from the command line with two subcommands, 
exactly like this:

python model.py train \
    --classifier random_forest \
    --in-reduced training_reduced.csv \
    --target target \
    --out-model model.joblib \
    --out-metrics training_metrics.json \
    --seed 42

python model.py predict \
    --input-testset test_reduced.csv \
    --target target \
    --model model.joblib \
    --out-predictions predictions.csv \
    --out-stats classification_stats.json

Additional constraints:
- Use scikit-learn, pandas, numpy, joblib.
- No absolute paths, no manual input required during execution.
- Must not require internet access or credentials.
- Include docstrings and type hints.
- The train/predict logic must not depend on the GUI in any way.

Please provide the full content of model.py.
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
Here is the complete model.py module.

Design choices:

1. Classifier registry: I used a simple dictionary mapping classifier name 
   strings to scikit-learn estimator constructors, so adding more classifiers 
   later is trivial. Chosen defaults:
   - "random_forest": RandomForestClassifier (mandatory per spec)
   - "logistic_regression": LogisticRegression (with increased max_iter, since 
     default 100 often fails to converge on real-world data)
   - "gradient_boosting": GradientBoostingClassifier
   All three support both .predict() and .predict_proba(), which is required 
   to produce the "score" column in predictions.

2. "score" column: defined as the predicted probability of the positive class 
   (target = 1), taken from predict_proba(X)[:, 1]. This matches the example in 
   the specification (higher score correlates with prediction = 1).

3. target_1_percentage: computed as (count of target==1 / n_samples) * 100, 
   consistent with the example values shown (e.g. 1.59, 1.56 suggest a 
   percentage, not a fraction).

4. classification_stats_json: built using sklearn.metrics 
   (precision_recall_fscore_support, accuracy_score, roc_auc_score, 
   confusion_matrix), matching field names and nesting exactly as specified.

5. CLI: implemented using argparse with subparsers ("train" and "predict"), as 
   required by the two distinct command-line invocations in the spec.

6. Reproducibility: seed is passed to every classifier that accepts a 
   random_state parameter, ensuring reproducible training.

"""
model.py

Classification model module: training and prediction for the QUBO-based
feature selection project.

Provides two mandatory functions:
- train(...): trains a classifier on the reduced training dataset and saves it.
- predict(...): applies a saved classifier to the reduced test dataset and
  saves predictions and classification statistics.

Can also be run from the command line with "train" and "predict" subcommands,
see `main()` below.
"""

import argparse
import json
import sys
import time
from typing import Any, Dict

import joblib
import numpy as np
import pandas as pd
from sklearn.ensemble import GradientBoostingClassifier, RandomForestClassifier
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import (
    accuracy_score,
    confusion_matrix,
    precision_recall_fscore_support,
    roc_auc_score,
)


def _build_classifier(classifier: str, seed: int) -> Any:
    """
    Instantiate a scikit-learn classifier by name.

    Parameters
    ----------
    classifier : str
        One of "random_forest", "logistic_regression", "gradient_boosting".
    seed : int
        Random seed passed as random_state where supported.

    Raises
    ------
    ValueError
        If the classifier name is not recognized.
    """
    registry = {
        "random_forest": lambda: RandomForestClassifier(
            n_estimators=200, random_state=seed, n_jobs=-1
        ),
        "logistic_regression": lambda: LogisticRegression(
            max_iter=1000, random_state=seed
        ),
        "gradient_boosting": lambda: GradientBoostingClassifier(random_state=seed),
    }

    if classifier not in registry:
        raise ValueError(
            f"Unknown classifier '{classifier}'. "
            f"Available options: {list(registry.keys())}"
        )

    return registry[classifier]()


def train(
    classifier: str,
    reducedTrain_csv: str,
    target_column: str,
    model_path: str,
    metrics_json: str,
    seed: int = 42,
) -> None:
    """
    Train a classifier on the reduced training dataset and save it, along with
    training statistics.

    Parameters
    ----------
    classifier : str
        Name of the classifier to train ("random_forest", "logistic_regression",
        or "gradient_boosting").
    reducedTrain_csv : str
        Path to the reduced training dataset (selected features + target).
    target_column : str
        Name of the target column.
    model_path : str
        Path where the trained model will be saved (joblib format).
    metrics_json : str
        Path where training statistics will be saved (JSON format).
    seed : int, optional
        Random seed for reproducibility. Default is 42.

    Raises
    ------
    ValueError
        If target_column is not found in the dataset, or classifier is unknown.
    """
    t0 = time.perf_counter()
    df = pd.read_csv(reducedTrain_csv)
    dataset_input_time = time.perf_counter() - t0

    if target_column not in df.columns:
        raise ValueError(
            f"Target column '{target_column}' not found in dataset columns: "
            f"{list(df.columns)}"
        )

    feature_columns = [c for c in df.columns if c != target_column]
    X = df[feature_columns].to_numpy()
    y = df[target_column].to_numpy()

    n_samples = len(df)
    n_features = len(feature_columns)
    target_1_percentage = float((y == 1).mean() * 100)

    model = _build_classifier(classifier, seed)

    t1 = time.perf_counter()
    model.fit(X, y)
    training_time = time.perf_counter() - t1

    joblib.dump(model, model_path)

    metrics = {
        "classifier": classifier,
        "seed": seed,
        "training_dataset": reducedTrain_csv,
        "target_column": target_column,
        "model_path": model_path,
        "n_samples": n_samples,
        "n_features": n_features,
        "target_1_percentage": round(target_1_percentage, 4),
        "dataset_input_time": round(dataset_input_time, 4),
        "training_time": round(training_time, 4),
    }

    with open(metrics_json, "w", encoding="utf-8") as f:
        json.dump(metrics, f, indent=4)


def predict(
    reduced_Test_csv: str,
    target_column: str,
    model_path: str,
    predictions_csv: str,
    classif_stats_json: str,
) -> None:
    """
    Apply a trained classifier to the reduced test dataset, saving per-record
    predictions and overall classification statistics.

    Parameters
    ----------
    reduced_Test_csv : str
        Path to the reduced test dataset (selected features + target).
    target_column : str
        Name of the target column.
    model_path : str
        Path to the saved trained model (joblib format).
    predictions_csv : str
        Path where per-record predictions will be saved (CSV format).
    classif_stats_json : str
        Path where classification statistics will be saved (JSON format).

    Raises
    ------
    ValueError
        If target_column is not found in the dataset.
    """
    df = pd.read_csv(reduced_Test_csv)

    if target_column not in df.columns:
        raise ValueError(
            f"Target column '{target_column}' not found in dataset columns: "
            f"{list(df.columns)}"
        )

    feature_columns = [c for c in df.columns if c != target_column]
    X = df[feature_columns].to_numpy()
    y_true = df[target_column].to_numpy()

    model = joblib.load(model_path)

    y_pred = model.predict(X)
    y_score = model.predict_proba(X)[:, 1]

    # --- Save per-record predictions ---
    predictions_df = pd.DataFrame(
        {
            "row_n": np.arange(len(df)),
            "target": y_true,
            "prediction": y_pred,
            "score": y_score,
        }
    )
    predictions_df.to_csv(predictions_csv, index=False)

    # --- Compute classification statistics ---
    n_samples = len(df)
    target_1_count = int((y_true == 1).sum())
    target_1_percentage = float((target_1_count / n_samples) * 100) if n_samples else 0.0

    accuracy = float(accuracy_score(y_true, y_pred))

    precision, recall, f1, support = precision_recall_fscore_support(
        y_true, y_pred, labels=[0, 1], zero_division=0
    )

    try:
        roc_auc = float(roc_auc_score(y_true, y_score))
    except ValueError:
        # roc_auc_score fails if y_true contains only one class
        roc_auc = None

    cm = confusion_matrix(y_true, y_pred, labels=[0, 1])

    classifier_name = getattr(model, "_qubo_classifier_name", type(model).__name__)

    stats: Dict[str, Any] = {
        "classifier": classifier_name,
        "n_samples": n_samples,
        "target_1_count": target_1_count,
        "target_1_percentage": round(target_1_percentage, 4),
        "accuracy": accuracy,
        "class_0": {
            "precision": float(precision[0]),
            "recall": float(recall[0]),
            "f1": float(f1[0]),
            "support": int(support[0]),
        },
        "class_1": {
            "precision": float(precision[1]),
            "recall": float(recall[1]),
            "f1": float(f1[1]),
            "support": int(support[1]),
        },
        "roc_auc": roc_auc,
        "confusion_matrix": {
            "labels": [0, 1],
            "matrix": cm.tolist(),
        },
    }

    with open(classif_stats_json, "w", encoding="utf-8") as f:
        json.dump(stats, f, indent=4)


def main() -> None:
    """Command-line interface for train and predict, via subcommands."""
    parser = argparse.ArgumentParser(
        description="Train a classifier or run predictions for the QUBO "
        "feature selection project."
    )
    subparsers = parser.add_subparsers(dest="command", required=True)

    # --- train subcommand ---
    train_parser = subparsers.add_parser("train", help="Train a classifier.")
    train_parser.add_argument("--classifier", required=True)
    train_parser.add_argument("--in-reduced", required=True, dest="in_reduced")
    train_parser.add_argument("--target", required=True)
    train_parser.add_argument("--out-model", required=True, dest="out_model")
    train_parser.add_argument("--out-metrics", required=True, dest="out_metrics")
    train_parser.add_argument("--seed", type=int, default=42)

    # --- predict subcommand ---
    predict_parser = subparsers.add_parser(
        "predict", help="Run predictions with a trained classifier."
    )
    predict_parser.add_argument(
        "--input-testset", required=True, dest="input_testset"
    )
    predict_parser.add_argument("--target", required=True)
    predict_parser.add_argument("--model", required=True)
    predict_parser.add_argument(
        "--out-predictions", required=True, dest="out_predictions"
    )
    predict_parser.add_argument("--out-stats", required=True, dest="out_stats")

    args = parser.parse_args()

    try:
        if args.command == "train":
            train(
                classifier=args.classifier,
                reducedTrain_csv=args.in_reduced,
                target_column=args.target,
                model_path=args.out_model,
                metrics_json=args.out_metrics,
                seed=args.seed,
            )
            print(f"Model saved to: {args.out_model}")
            print(f"Training metrics saved to: {args.out_metrics}")

        elif args.command == "predict":
            predict(
                reduced_Test_csv=args.input_testset,
                target_column=args.target,
                model_path=args.model,
                predictions_csv=args.out_predictions,
                classif_stats_json=args.out_stats,
            )
            print(f"Predictions saved to: {args.out_predictions}")
            print(f"Classification statistics saved to: {args.out_stats}")

    except (FileNotFoundError, ValueError) as e:
        print(f"ERROR: {e}")
        sys.exit(1)


if __name__ == "__main__":
    main()


Notes:
- One caveat worth flagging in your report: the "classifier" field in the 
  predict() output JSON is derived from the loaded model's Python class name 
  (e.g. "RandomForestClassifier"), since predict() only receives the model file, 
  not the original classifier name string used at training time. If you prefer 
  it to exactly match the original lowercase name used in train() (e.g. 
  "random_forest"), we can persist that name alongside the model (e.g. by 
  saving a small wrapper object or a sidecar metadata file). Let me know if you 
  want me to adjust this.
- Run training as:
    python src/qubo_project/model.py train --classifier random_forest \
        --in-reduced outputs/training_reduced.csv --target <target_name> \
        --out-model outputs/model.joblib --out-metrics outputs/training_metrics.json \
        --seed 42
- Run prediction as:
    python src/qubo_project/model.py predict \
        --input-testset outputs/test_reduced.csv --target <target_name> \
        --model outputs/model.joblib --out-predictions outputs/predictions.csv \
        --out-stats outputs/classification_stats.json
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G25-07-17-001_model_function.py"
    repository_path: "reports/llm_usage/files/LLM_G25-07-17-001_model_function.py"
    file_type: "py"       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: "Training e classificazione"
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: false  # true / false
  modified_before_use: true   # true / false
  description_of_modifications: "I modified the provided code based on a subsequent interaction with the LLM (see G25-07-17-002)"
  related_repository_files:
    - src/qubo_project/model.py
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "yes"    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones    
    - "other"
  description: "In this response, the predict() function derived the 'classifier' field from the Python class name of the loaded
    model (e.g. 'RandomForestClassifier') instead of the original lowercase name passed to train() (e.g. 'random_forest'), causing
    inconsistency between training_metrics.json and classification_stats.json"
  how_issue_was_resolved: "Based on a subsequent interaction (G25-07-17-002), I modified train() to save a dict {model
    classifier_name} using joblib instead of the raw model object, and predict() to unpack it and use classifier_name directly"
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 4
  correctness_1_to_5: 4
  clarity_1_to_5: 4
  confidence_after_verification_1_to_5: 4
  would_reuse_this_output: 4
  notes: "The code works but there is a little issue"
```

---

## Interaction  G25-07-17-002

### 1. LLM and chat information

```yaml
llm_name: "Claude"
llm_version_or_model: "Claude Sonnet 5"
chat_name_or_identifier: "Debug of the previous interaction"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "66454"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "classification_model"
  - "debugging"  
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: "yes"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: "G25-07-17-001" 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
In model.py, the predict() function saves the classifier name in 
classification_stats.json by reading type(model).__name__, so it writes 
something like "RandomForestClassifier" instead of "random_forest". But in 
train(), the training_metrics.json correctly saves "random_forest", since it 
uses the classifier variable directly.

This means the two JSON files show a different name for the same classifier. 
I want both files to always show the same lowercase name (e.g. "random_forest") 
that was originally passed to train().

Can you fix this? I don't want to change the function signatures or the CLI.
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
The problem is that train() and predict() are run separately, and the only 
thing passed between them is the .joblib file. Right now that file only 
contains the trained model object, so predict() has no way to know which 
name you originally used.

Fix: instead of saving just the model, save a small dictionary containing 
both the model and the classifier name string, then unpack it in predict().

In train(), replace:
    joblib.dump(model, model_path)
with:
    joblib.dump({"model": model, "classifier_name": classifier}, model_path)

In predict(), replace:
    model = joblib.load(model_path)
with:
    saved_obj = joblib.load(model_path)
    model = saved_obj["model"]
    classifier_name = saved_obj["classifier_name"]

And remove this line, which is no longer needed:
    classifier_name = getattr(model, "_qubo_classifier_name", type(model).__name__)

Then use classifier_name (already available) when building the stats dictionary, 
instead of recomputing it from the model object.

This keeps both function signatures and the CLI exactly as they were, and 
guarantees training_metrics.json and classification_stats.json always report 
the same classifier name.
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G25-07-17-002_model_function_fix.py"
    repository_path: "reports/llm_usage/files/LLM_G25-07-17-002_model_function_fix.py"
    file_type: "py"       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: "Fix of model function"
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true  # true / false
  modified_before_use: false   # true / false
  description_of_modifications: null
  related_repository_files:
    - src/qubo_project/model.py
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"   
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: 5
  notes: "The response fix the previous issue"
```

---

## Interaction  GXX-MM-DD-003

Copy the structure of `GXX-MM-DD-001` for every new interaction.

---

# Suggested file naming convention

Use unique and descriptive names for every file generated or downloaded from an LLM response.

Recommended format:

```text
LLM_<interaction_id>_<short_description>.<extension>
```

Examples:

```text
LLM_G03-06-05-002_preprocessing_function.py
LLM_G03-06-12-001_qubo_cost_function.py
LLM_G03-06-12-001_test_feature_selection.py
LLM_G03-06-13-003_readme_section.md
```

---

# Final checklist

Before submission, verify that:

- [ ] Every relevant LLM interaction has been recorded.
- [ ] Each interaction has a unique ID.
- [ ] Date and time are present for every interaction.
- [ ] The author is identified as either `couple` or one student matricola.
- [ ] The full prompt is included.
- [ ] The full LLM response is included or correctly referenced as a separate file.
- [ ] Every downloaded/generated file has a unique name, is present in the repository and is correctly referenced in this log.
- [ ] Problems, errors, and hallucinations have been reported honestly.
